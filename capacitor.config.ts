import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Project Genesis',
  webDir: 'dist',
  server: {
    cleartext: true
  }
};

export default config;
